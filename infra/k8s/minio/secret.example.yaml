# Template — DO NOT put real credentials here.
# Copy to secret.yaml and fill in real values (gitignored).
#   cp secret.example.yaml secret.yaml
apiVersion: v1
kind: Secret
metadata:
  name: vn-minio-root
  namespace: vn-storage
type: Opaque
stringData:
  # Root admin credentials. App-level access keys should be created
  # separately (via mc admin user add) and stored in their own Secret.
  rootUser: <REPLACE_ME_USERNAME>
  rootPassword: <REPLACE_ME_PASSWORD>
