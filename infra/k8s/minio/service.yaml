apiVersion: v1
kind: Service
metadata:
  name: vn-minio
  namespace: vn-storage
spec:
  clusterIP: None
  selector:
    app: vn-minio
  ports:
    - name: s3-api
      port: 9000
      targetPort: 9000
    - name: console
      port: 9001
      targetPort: 9001
