# Template — DO NOT put real passwords here.
# Copy to app-user-secret.yaml and fill in a real password (gitignored).
#   cp app-user-secret.example.yaml app-user-secret.yaml
apiVersion: v1
kind: Secret
metadata:
  name: vn-postgres-app-user
  namespace: vn-data
type: kubernetes.io/basic-auth
stringData:
  username: vn_app
  password: <REPLACE_ME>
